---
description: 前端架构约束（微前端、Taro/多端、全局状态管理）
globs: "**/*.{ts,tsx,js,jsx,vue}"
alwaysApply: false
---

# 前端项目架构约束

> 全库 Skills/Agents 索引见 [`cursor-workspace-index.mdc`](./cursor-workspace-index.mdc)。


以下约束在对应技术栈**已引入或维护子项目**时执行；与《前端项目通用编码规范》规则同时生效，冲突时以**主应用/子应用 README 与工程约定**为准。

## 1. 微前端（qiankun / 无界 Wujie）

- **子应用入口**：按主应用约定导出 **`bootstrap` / `mount` / `unmount`**，生命周期与主应用配置一致，禁止遗漏或空实现影响卸载与重复挂载。
- **全局与样式**：不随意在子应用里**改挂 `window` 上的共享名**；样式使用 **scoped / CSS Modules / BEM+子应用命名空间** 等，避免与主应用、其他子应用 **样式污染**。
- **路由**：子应用使用 **`base`**（或工程规定的 `publicPath` / `basename`），路径前缀与主应用**注册名一致**，禁止与主应用已有路由**冲突或抢占同前缀**。
- **静态资源**：构建产物中资源引用使用**相对/由构建注入的 base**，避免写死**站点根绝对路径**导致在主应用子路径下 404。

## 2. Taro / 多端（微信 + H5 等）

- **双端能力**：以 Taro 及官方文档为准；**禁止**在业务路径直接用仅一端存在的 API 而不做**条件编译或封装**（`process.env.TARO_ENV`、封装层分端等）。
- **生命周期**：使用 **Taro 页面/组件**生命周期（如 `useLoad` / `useReady` 等，随项目所选版本与规范），**禁止**依赖**仅浏览器**的页面生命周期当唯一数据源。
- **TabBar**：**tabBar** 在 `app.config`（或项目统一配置）中维护，**不得**为局部需求私改项而不走评审/文档。
- **布局**：小程序端须考虑**不同屏宽与 DPR**；**禁止**关键布局只依赖**固定 px** 而不做 rpx/弹性布局/安全区等适配（与设计稿与项目适配策略一致）。

## 3. 状态管理

- **全局状态**：跨页面/多组件共享的域状态，经团队选型的 **Redux / Zustand / Pinia**（或项目文档指定的**唯一**方案）管理；**禁止**为少次数跨层用「层层 props 传递」当长期方案，应上提 store 或组合式 API。
- **范围**：**优先**组件/页面内 **`useState` / 局部 store**；仅当状态**多消费者、跨路由或明确域边界**时进入全局层。
- **更新**：状态更新须 **immutable**（新对象/新数组/库提供的 `produce` 等），**禁止**对已有 state **原地 mutate** 再触发“更新”（除非与所用库 API 的明确要求一致，且文档已约定）。
