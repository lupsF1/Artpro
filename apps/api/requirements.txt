# FastAPI stack (方案 B) — lock major versions in production via pip-compile if needed
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.20.0
alembic>=1.13.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
email-validator>=2.0.0
pyjwt[crypto]>=2.8.0
bcrypt>=4.0.0
# sync driver for Alembic migrations
psycopg[binary]>=3.1.0

# tests (pytest 在 apps/api 下执行)
pytest>=8.0.0
